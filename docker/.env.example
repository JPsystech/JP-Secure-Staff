# Copy to docker/.env or set in shell when running docker compose.
# Used by docker-compose.yml for postgres and backend.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=jp_secure_staff

SECRET_KEY=change-me-in-production-min-32-characters
