# JP Secure Staff - Frontend env template. Copy to .env.local for Next.js.
# API base URL (no trailing slash; backend must allow this origin in CORS/ALLOWED_ORIGINS)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
